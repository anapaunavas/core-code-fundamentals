# Week 7 challenges (tuesday)

**Objects**
```
function animal(obj){
  return `This ${obj.color} ${obj.name} has ${obj.legs} legs.`;
}
```

**Return to sanity**

```
function mystery() {
  var results =
    {sanity: 'Hello'};
  return results;
    results;
}
```
